description: Concurrent request with the same idempotency key is currently processing.
content:
  application/json:
    schema:
      type: object
      properties:
        error:
          type: string
          example: "idempotency_conflict"
        message:
          type: string
          example: "A request with this idempotency key is currently executing."
          